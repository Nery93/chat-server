# Optional override for the WebSocket server address.
# Not shown anywhere in the UI — only used to build the connection URL.
# Defaults to ws://localhost:8080 if not set.
VITE_WS_URL=ws://localhost:8080
